# =============================================================================
# my-wedding-rsvp — local environment
# =============================================================================
# Copy to .env.local for local development.
# =============================================================================

# PlanMyDay API base URL (no trailing slash)
NEXT_PUBLIC_PLANNER_API_URL=http://localhost:3000

# PlanMyDay dashboard origin allowed to embed this site in an iframe
NEXT_PUBLIC_PLANNER_APP_URL=http://localhost:3000
